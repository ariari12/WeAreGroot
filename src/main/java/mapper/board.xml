<?xml version="1.0" encoding="UTF-8"?>	<!DOCTYPE mapper
	  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.moran.board">
	<select id="findAll" resultType="kr.co.moran.web.vo.BoardVO">
		SELECT b_id, m_id, b_title, b_contents, b_regdate, b_view_cnt, b_like_cnt, b_type, b_is_maintain, b_retention_period 
		FROM BOARD
	</select id="findAll">
	
	
</mapper>
